# SPOLINK 서비스 정책서 v0.1

## 문서 목적

SPOLINK MVP에서 구현 전에 고정해야 할 서비스 운영 정책을 정의한다. 이 문서는 ERD, API 명세, 화면 설계, 결제/정산 구현의 기준 문서다.

## 기준 결정

- MVP 아키텍처는 `Supabase-first`로 시작한다.
- 인증은 `Supabase Auth`를 기준으로 한다.
- DB는 Supabase PostgreSQL을 기준으로 한다.
- 파일 저장은 Supabase Storage를 기준으로 한다.
- 실시간 예약 상태, 채팅, 알림 이벤트는 Supabase Realtime 또는 Edge Functions 기반으로 검토한다.
- 결제는 Toss Payments를 기준으로 한다.
- 프론트엔드는 Next.js, React, TypeScript, Tailwind CSS, shadcn/ui를 기준으로 한다.
- NestJS, Prisma, Redis, Socket.IO는 MVP 이후 서버 복잡도가 커질 때 재검토한다.

## 서비스 범위

### MVP 포함

- 회원가입 및 로그인
- 학습자/지도자 역할 관리
- 지도자 자격 인증 신청 및 관리자 승인
- 레슨 등록, 검색, 상세 조회
- 예약 신청, 결제, 예약 확정
- 예약 취소 및 환불
- 수업 완료 처리
- 리뷰 작성
- 마이페이지
- 기본 신고 및 차단

### MVP 제외

- 운동 메이트
- 운동 기록
- GPS/QR 출석 자동화
- 배지와 목표 관리
- 푸시 전달과 고급 알림 자동화
- 모바일 앱
- 프리미엄 지도자 구독
- 광고 및 제휴 상품

## 사용자 정책

### 사용자 유형

| 유형 | 설명 | MVP 권한 |
|------|------|----------|
| 게스트 | 로그인하지 않은 방문자 | 레슨 검색, 레슨 상세 일부 조회 |
| 학습자 | 레슨을 예약하는 사용자 | 예약, 결제, 리뷰, 찜, 신고 |
| 지도자 | 레슨을 등록하고 운영하는 사용자 | 인증 신청, 레슨 등록, 예약 관리, 정산 조회 |
| 관리자 | 서비스 운영자 | 지도자 승인, 신고 처리, 예약/결제/정산 운영 확인 |

### 회원 가입

- 이메일 기반 가입을 기본으로 한다.
- 소셜 로그인은 Supabase Auth 지원 범위 안에서 추가할 수 있다.
- 사용자는 가입 후 기본적으로 학습자 권한을 가진다.
- 지도자 권한은 별도 인증 승인 후 부여한다.

### 계정 상태

| 상태 | 의미 |
|------|------|
| active | 정상 이용 가능 |
| pending_coach | 지도자 인증 심사 중 |
| coach_approved | 지도자 활동 가능 |
| suspended | 신고, 결제 분쟁, 운영 위반 등으로 일시 제한 |
| deleted | 탈퇴 또는 관리자 삭제 처리 |

### 탈퇴

- 탈퇴 시 로그인과 신규 예약은 즉시 제한한다.
- 진행 중 예약, 결제, 환불, 정산이 있으면 완료 후 개인정보 보관 정책에 따라 삭제 또는 비식별 처리한다.
- 법령 또는 분쟁 대응에 필요한 결제/정산 기록은 보관 기간을 별도로 둔다.

## 지도자 인증 정책

### 인증 대상

지도자로 활동하려는 사용자는 다음 정보를 제출한다.

- 실명
- 연락처
- 프로필 사진
- 활동 지역
- 전문 종목
- 스포츠지도사 등 자격증 파일(PNG, JPEG, PDF)
- 경력 설명
- 정산 계좌 요약 정보(은행명, 계좌 끝 4자리, 예금주)

신청 계약:

- 기존 프로필의 실명, 연락처, 프로필 사진이 완료되어야 심사에 제출할 수 있다.
- 신청서에는 활동 지역, 전문 종목, 한 줄 소개, 경력 설명, 경력 연수와 정산 계좌 요약
  정보를 입력하고 자격증 파일을 1개 이상 등록한다.
- 자격증 파일은 파일당 10MB 이하의 PNG, JPEG, PDF만 허용하며 MIME, 확장자와 magic
  bytes가 일치해야 한다.
- 자격증은 비공개 `coach-certificates` Storage에 저장한다. 신청자 본인과 활성 관리자만
  정해진 경로로 접근하며 공개 URL은 만들지 않는다.
- 전체 계좌번호 수집과 자격증 보관·삭제 정책은 후속 정책으로 확정한다. 현재 구현은
  실패한 메타데이터 등록의 업로드 객체만 보상 삭제하고, 제출 이후 파일을 임의 삭제하지 않는다.

### 심사 기준

관리자는 다음 항목을 확인한다.

- 자격증 이미지 식별 가능 여부
- 이름과 제출 정보 일치 여부
- 전문 종목과 자격/경력의 관련성
- 부적절한 프로필 이미지 또는 소개 문구 여부
- 중복 계정 또는 이전 제재 이력 여부

### 인증 상태

| 상태 | 의미 |
|------|------|
| draft | 제출 전 작성 중 |
| submitted | 심사 요청 완료 |
| approved | 지도자 활동 가능 |
| rejected | 반려됨 |
| suspended | 승인 후 활동 제한 |

계정/신청 상태는 다음 조합을 사용한다. 모든 신청자의 `profiles.role`은 `learner`로
유지하며 브라우저가 역할이나 상태를 지정할 수 없다.

| 인증 상태 | 계정 상태 |
|-----------|-----------|
| draft, rejected | active |
| submitted | pending_coach |
| approved | coach_approved |
| suspended | suspended |

### 반려

- 반려 시 사유를 사용자에게 표시한다.
- 사용자는 정보를 수정해 재신청할 수 있다.
- 허위 자격증, 도용, 반복 허위 제출은 계정 제재 사유다.
- 제출과 심사 결정은 서버의 원자적 전이만 허용한다. 같은 결정의 재시도는 멱등이며,
  반대 결정이나 오래된 상태의 요청은 충돌로 거절한다.
- 승인/반려는 신청서와 계정 상태, 감사 로그와 앱 내 알림을 한 트랜잭션으로 기록한다.

## 레슨 정책

### 레슨 등록 필수 정보

- 종목
- 제목
- 설명
- 지도자
- 장소 또는 활동 지역
- 진행 방식
- 수업 시간
- 가격
- 정원
- 준비물
- 취소 가능 기준

### 레슨 상태

| 상태 | 의미 |
|------|------|
| draft | 작성 중 |
| pending_review | 관리자 검토 필요 |
| active | 예약 가능 |
| paused | 지도자가 일시 중지 |
| closed | 예약 종료 |
| rejected | 관리자 반려 |

저장 상태를 API 입력으로 받지 않는다. 지도자는 `submit`, `pause`, `resume`, `close`, 관리자는
`approve`, `reject` 명령만 보낼 수 있고, 서버 workflow가 현재 상태를 잠근 뒤 다음 상태를 파생한다.
같은 명령의 동일 재시도만 멱등이며 반대 명령이나 오래된 상태는 충돌로 처리한다.

### 레슨 노출

- 인증 승인된 지도자의 `active` 레슨만 검색에 노출한다.
- 레슨과 일정 작성/수정은 지도자 신청과 계정 상태가 각각 `approved`, `coach_approved`인 본인만
  가능하다. 일반 사용자와 관리자도 상태·심사자·시각을 테이블에 직접 기록할 수 없다.
- 가격, 장소, 시간, 정원 정보가 없는 레슨은 노출하지 않는다.

### 레슨 이미지

- 이미지는 선택 사항이며 레슨당 JPEG/PNG/WebP 최대 5장, 파일당 `1..5,242,880` bytes
  (5 MiB)를 허용한다. 서버가 만든 immutable object name만 사용하고 첫 순서 `0`을 표지로 한다.
- 승인된 지도자는 본인 `draft|rejected` 레슨에서만 intent 생성, 등록, 조밀한 순서 변경, 삭제를
  수행한다. 브라우저는 bucket/path, 소유자, 순서, lifecycle 상태를 지정하거나 테이블을 직접 쓰지 않는다.
- 새 레슨은 초안을 한 번 저장한 뒤 선택 순서대로 직렬 업로드한다. 부분 실패 시 등록 완료 이미지는
  유지하고 실패/남은 파일만 같은 초안에 재시도하여 중복 초안이나 중복 파일을 만들지 않는다.
- 등록 전 서버가 실제 Storage 객체의 크기, MIME, 확장자와 magic bytes를 재검증한다. 실패한
  객체/intent cleanup과 없는 객체 삭제는 멱등이며, 물리 삭제 실패는 `deleting` 영수증으로 재시도한다.
- public `lesson-images` bucket을 사용하므로 앱 DB/API/화면에서는 삭제 즉시 제외하지만 이미 알려진
  public CDN URL이 모든 캐시에서 즉시 회수된다고 보장하지 않는다.
- 로컬 MVP는 `corepack pnpm lesson-images:cleanup`과 이미지 mutation의 bounded 기회적 cleanup을
  제공한다. Hosted Supabase 배포와 정기 scheduler는 별도 후속 작업이며 현재 운영 기능이 아니다.
- 신고 누적, 허위 정보, 안전 문제 발생 시 관리자가 레슨을 중지할 수 있다.

## 예약 정책

### 예약 흐름

```text
레슨 선택
-> 일정 선택
-> 예약 정보 확인
-> 결제 요청
-> 결제 완료
-> 예약 확정
-> 수업 진행
-> 수업 완료
-> 리뷰 작성
-> 정산 대기
-> 정산 완료
```

### 예약 상태

| 상태 | 의미 |
|------|------|
| pending_payment | 결제 전 임시 예약 |
| confirmed | 결제 완료 및 예약 확정 |
| cancelled_by_user | 학습자 취소 |
| cancelled_by_coach | 지도자 취소 |
| cancelled_by_admin | 관리자 취소 |
| completed | 수업 완료 |
| no_show_user | 학습자 노쇼 |
| no_show_coach | 지도자 노쇼 |
| disputed | 분쟁 접수 |

### 임시 예약

- 결제 전 임시 예약은 짧은 제한 시간을 둔다.
- 제한 시간 안에 결제가 완료되지 않으면 자동 만료한다.
- 동일 시간대 정원 초과 예약을 막기 위해 예약 가능 수량은 서버 기준으로 확인한다.

### 예약 확정

- 결제 성공 이벤트가 확인된 뒤 `confirmed`로 전환한다.
- 예약 확정 후 학습자와 지도자에게 예약 정보를 표시한다.
- 지도자는 확정 예약을 임의 삭제할 수 없고 취소 정책을 따라야 한다.

## 결제 정책

### 결제 수단

- Toss Payments를 MVP 결제 수단으로 사용한다.
- 결제 성공 여부는 클라이언트 응답만 믿지 않고 서버 또는 Edge Function에서 검증한다.

### 결제 상태

| 상태 | 의미 |
|------|------|
| ready | 결제 요청 생성 |
| paid | 결제 승인 완료 |
| failed | 결제 실패 |
| cancelled | 결제 취소 |
| partially_refunded | 부분 환불 |
| refunded | 전액 환불 |

### 결제 검증

- 예약 금액과 결제 금액이 일치해야 한다.
- 예약자와 결제 요청자가 일치해야 한다.
- 이미 결제 완료된 예약에 중복 결제를 허용하지 않는다.
- 결제 검증 실패 시 예약은 확정하지 않는다.

## 취소 및 환불 정책

### 학습자 취소

취소 시점은 서버가 취소 요청을 수락한 시각부터 `lesson_schedules.starts_at`까지 남은 시간으로 계산한다. 학습자 환불액은 수수료 차감 전 총 예약금액인 `reservations.reserved_price_amount`에 아래 비율을 적용하고, 계산 결과의 1원 미만 소수점은 버림한다.

| 수업 시작까지 남은 시간 | 환불 기준 |
|-------------------------|-----------|
| 24시간 이상 (`remaining >= 24h`) | 총 예약금액의 70% 환불 |
| 3시간 이상 24시간 미만 (`3h <= remaining < 24h`) | 총 예약금액의 50% 환불 |
| 3시간 미만 (`remaining < 3h`) | 환불 불가, 0% |

- 정확히 24시간이 남은 경우 70%, 정확히 3시간이 남은 경우 50% 구간을 적용한다.
- 예: `reserved_price_amount = 10,001원`이고 70% 구간이면 `7,000.7원`의 소수점을 버림해 `7,000원`을 환불한다.
- 취소 트랜잭션은 환불 대상 금액이 0원보다 클 때 내부 환불 요청만 생성한다. 실제 Toss Payments 환불은 후속 Edge 처리 단계에서 수행한다.

### 지도자 취소

- 지도자가 취소하면 학습자에게 전액 환불한다.
- 지도자 취소 환불액은 `reserved_price_amount`의 100%다.
- 반복 취소 지도자는 검색 노출 제한 또는 활동 제한 대상이다.
- 지도자 사유 취소는 정산 대상에서 제외한다.

### 관리자 취소

관리자는 다음 상황에서 예약을 취소할 수 있다.

- 허위 레슨
- 안전 문제
- 결제 오류
- 신고 또는 분쟁
- 서비스 장애

관리자가 취소하면 `reserved_price_amount`의 100%를 환불한다.

### 예외 환불

천재지변, 시설 폐쇄, 긴급 질병 등 불가피한 사유는 관리자가 증빙을 확인해 예외 환불할 수 있다.

## 노쇼 정책

### 학습자 노쇼

- KST 일정 시작 instant로부터 정확히 15분이 지난 뒤에도 학습자가 나타나지 않으면
  `no_show_user`로 처리할 수 있다. 판정에는 서버 시각만 사용한다.
- 학습자 노쇼는 원칙적으로 환불하지 않는다.
- 반복 노쇼는 예약 제한 사유가 될 수 있다.

### 지도자 노쇼

- 지도자가 수업을 진행하지 않으면 학습자에게 전액 환불한다.
- 지도자 노쇼는 관리자 검토 후 정산 제외, 노출 제한, 활동 정지 사유가 된다.
- 노쇼 요청은 `mark_learner_no_show` 또는 `mark_coach_no_show` 명령과 bounded 사유만 받는다.
  저장 상태, actor, 처리 시각, 환불·정산 금액은 잠근 예약/결제 행에서 서버가 파생한다.

### 출석 확인

- MVP에서는 지도자와 학습자의 수동 완료 확인을 우선한다.
- GPS/QR 출석은 Phase 2에서 도입한다.

## 정산 정책

### 정산 기준

- 정산은 수업 완료 후 환불/분쟁 가능 기간이 지난 예약을 기준으로 한다.
- 결제 수수료, 플랫폼 수수료, 환불 금액을 제외한 금액을 지도자에게 지급한다.

### 정산 상태

| 상태 | 의미 |
|------|------|
| pending | 정산 대기 |
| hold | 분쟁 또는 검토로 보류 |
| approved | 정산 승인 |
| paid | 지급 완료 |
| failed | 지급 실패 |

local MVP에서 실행 가능한 정산 명령은 `approve`, `hold`뿐이며 상태는 `pending`, `hold`,
`approved`까지만 전이한다. `paid`, `failed`는 향후 payout provider workflow를 위한 예약 상태다.
수수료율이 결정되기 전에는 금액을 추정하지 않고, 생성된 행은
`net = gross - platform fee - payment fee - refund`와 공제 합계가 gross 이하인 조건을 만족한다.

### 정산 보류

다음 경우 정산을 보류한다.

- 수업 완료 여부 분쟁
- 지도자 노쇼 의심
- 결제 취소 또는 환불 진행 중
- 신고 접수
- 계좌 정보 오류

## 리뷰 정책

### 작성 조건

- 리뷰는 `completed` 예약의 학습자만 작성할 수 있다.
- 하나의 예약에는 하나의 리뷰만 작성한다.
- 지도자는 자신이 운영한 레슨에 리뷰를 작성할 수 없다.

### 리뷰 내용 제한

금지되는 내용은 다음과 같다.

- 욕설과 혐오 표현
- 개인정보 노출
- 허위 사실
- 광고와 외부 유도
- 수업과 무관한 내용

### 리뷰 상태

| 상태 | 의미 |
|------|------|
| visible | 노출 중 |
| hidden | 신고 또는 관리자 판단으로 숨김 |
| deleted | 작성자 삭제 또는 관리자 삭제 |

- 작성자는 본인의 `visible`과 `hidden` 리뷰를 모두 조회하며, `hidden`의 숨김 사유는 작성자 본인에게만 표시한다.
- `deleted` 리뷰는 작성자를 포함한 일반 사용자에게 제공하지 않는다.

## 신고 및 차단 정책

### 신고 대상

- 사용자
- 지도자
- 레슨
- 리뷰
- 예약 건

채팅이 구현되지 않은 MVP에서는 메시지 신고를 API와 DB 모두에서 허용하지 않는다.

### 신고 사유

- 허위 정보
- 불쾌하거나 위험한 행동
- 결제 또는 환불 분쟁
- 욕설, 혐오, 성희롱
- 개인정보 침해
- 외부 거래 유도

### 처리 상태

| 상태 | 의미 |
|------|------|
| submitted | 신고 접수 |
| reviewing | 검토 중 |
| resolved | 조치 완료 |
| rejected | 신고 사유 부족 |

### 차단

- 사용자는 다른 사용자를 차단할 수 있다.
- 차단 시 채팅과 상호작용을 제한한다.
- 이미 확정된 예약이 있으면 관리자 정책에 따라 별도 처리한다.

## 개인정보 및 위치정보 정책

### 개인정보

MVP에서 다루는 개인정보는 다음과 같다.

- 이름
- 이메일
- 연락처
- 프로필 이미지
- 지도자 자격증 이미지
- 정산 계좌 정보
- 예약 및 결제 기록

### 위치정보

- 위치 기반 검색은 사용자 동의 후 사용한다.
- MVP에서는 현재 위치 또는 사용자가 입력한 지역을 기준으로 검색한다.
- 정확한 실시간 위치 추적은 MVP 범위에서 제외한다.

### 보관 원칙

- 서비스 제공에 필요한 최소 정보만 저장한다.
- 자격증, 결제, 정산, 분쟁 기록은 목적별 보관 기간을 정의해야 한다.
- 공개 프로필과 내부 검증 정보는 분리한다.

## 알림 정책

MVP 알림 대상은 다음으로 제한한다.

- 회원가입 및 로그인 관련 안내
- 지도자 인증 접수, 승인, 반려
- 예약 확정
- 예약 취소
- 결제 완료
- 수업 전 리마인드
- 리뷰 작성 요청
- 신고 처리 결과

현재 구현은 트랜잭션 안에서 중복 없이 기록하는 앱 내 알림 목록/읽음 처리까지다. 푸시
전달은 Firebase Cloud Messaging 등 provider 연동 전까지 deferred다.

앱 내 알림의 `data`는 type별 연결 ID와 상태 allowlist만 저장한다. 이메일, 전화, 주소, provider
key, token/cookie, raw 결제/환불 payload와 중첩 객체/배열은 저장하거나 응답하지 않는다.

## 관리자 운영 정책

관리자는 다음 작업을 수행할 수 있어야 한다.

- 지도자 인증 승인/반려
- 레슨 숨김 또는 중지
- 신고 검토
- 예약/결제/환불 상태 확인
- 정산 보류 및 승인
- 리뷰 숨김
- 계정 일시 제한

관리자 작업은 감사 로그를 남긴다. 활성 관리자도 보호 테이블을 직접 수정하지 않고 승인된 action
RPC를 사용한다. reviewer/actor, 처리 시각, 상태, 환불·정산 금액은 claims와 잠근 DB 행에서 파생한다.
동일 action의 동일 입력 재시도만 멱등이며 stale 또는 반대 결정은 충돌로 거절한다.

## 데이터 모델에 필요한 상태값

ERD 작성 시 최소한 다음 상태값을 반영한다.

- `users.status`
- `coach_profiles.status`
- `lessons.status`
- `reservations.status`
- `payments.status`
- `settlements.status`
- `reviews.status`
- `reports.status`

## API 설계에 필요한 우선 엔드포인트

API 명세서 작성 시 MVP 1차 범위는 다음 순서로 설계한다.

1. 인증 세션 조회
2. 내 프로필 조회/수정
3. 지도자 인증 신청/조회
4. 레슨 생성/수정/검색/상세
5. 예약 가능 시간 조회
6. 예약 생성
7. 결제 요청 생성
8. 결제 검증
9. 예약 취소
10. 수업 완료 처리
11. 리뷰 작성
12. 신고 접수
13. 관리자 지도자 승인/반려

## 미결정 사항

- 플랫폼 수수료율
- 정산 주기
- 결제 수수료 부담 주체
- 환불 수수료 부담 기준
- 지도자 자격증 검증 자동화 여부
- 관리자 승인 SLA
- 신고 누적 기준과 자동 제재 기준
- 법정 개인정보 보관 기간

## 다음 문서 작업

1. `SPOLINK_ERD.md`
2. `SPOLINK_API_명세서.md`
3. `SPOLINK_화면_설계.md`
4. `SPOLINK_디자인_시스템.md`
